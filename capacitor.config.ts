import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.myersguo.simpleenglish',
  appName: 'Simple English',
  webDir: 'dist'
};

export default config;
